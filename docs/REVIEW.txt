Rust vs Python Parity Review
Date: 2026-02-11
Scope: Full re-review of Rust implementation against Python function/class parity target.

Summary
- API presence parity remains complete (102/102 entries mapped).
- Major runtime gaps were re-audited and implemented in this pass.
- Remaining gaps are now mostly protocol/provider edge-case fidelity.

Findings (Python has, Rust not fully matching yet)

1) Streaming LLM delta semantics
- Python status:
  - `LLMClient.chat_completion` supports streaming events (`TEXT_DELTA`, tool-call deltas, message complete).
- Rust status:
  - Added SSE stream parsing with delta/tool-call chunk handling plus non-stream fallback.
- Gap:
  - Core parser and provider fixture tests are in place; remaining work is larger provider matrix coverage.
- Priority: High
- Current state: DONE (with edge-case hardening still possible)

2) MCP stdio/SSE transport fidelity
- Python status:
  - Uses `fastmcp` client transports with real MCP protocol calls and `list_tools`.
- Rust status:
  - HTTP JSON-RPC path implemented (`initialize`, `tools/list`, `tools/call`).
  - stdio JSON-RPC path implemented (`initialize`, `tools/list`, `tools/call`).
  - SSE request/response parsing path implemented for MCP event streams.
- Gap:
  - Long-lived SSE session behavior across diverse MCP servers still needs broader integration tests.
- Priority: High
- Current state: PARTIAL

3) Tool discovery runtime registration
- Python status:
  - Dynamic tool discovery manager loads discovered tools into registry.
- Rust status:
  - Discovery scans manifests and now builds/registers executable discovered tools.
- Gap:
  - Manifest schema is practical but not fully standardized with external ecosystems.
- Priority: Medium
- Current state: DONE

4) Context compaction strategy depth
- Python status:
  - More advanced compaction flow integrated with token budgeting and context pruning behavior.
- Rust status:
  - Improved role-aware truncation implemented.
- Gap:
  - Lacks full parity around nuanced pruning/compression strategy and integration points.
- Priority: Medium
- Current state: PARTIAL

5) TUI rendering fidelity
- Python status:
  - Rich formatted panels, syntax blocks, detailed tool visuals.
- Rust status:
  - Plain-text TUI behavior.
- Gap:
  - Reduced UX and output formatting fidelity.
- Priority: Low
- Current state: PARTIAL

Implemented During This Review Pass
- `approval.rs`
  - Added interactive confirmation prompt for `NeedsConfirmation` operations.
- `tools/subagents.rs`
  - Replaced placeholder with real subagent execution (nested agent run + timeout + result/tool summary).
- `tools_discovery.rs`
  - Added manifest-based discovery scan and executable tool build path.
- `tools/discovered_tool.rs`
  - Added dynamic discovered tool execution runtime.
- `mcp.rs`
  - Added JSON-RPC stdio/http flow for initialize, list tools, and call tool.
- `client_llm.rs`
  - Added stream parser hardening and tests for chunk/tool-call accumulation behavior.
  - Added provider-specific fixture tests for noisy fields and malformed argument fragments.
- `context_compaction.rs`
  - Added role-aware compaction and token-bounded truncation behavior.
- `config_loader.rs`
  - Added system/project TOML loading and partial config overlay merge.

Next Implementation Targets
1. Add integration-style MCP SSE server fixture tests (long-lived stream/session).
2. Expand stream fixtures for additional provider chunk variants (beyond current fixtures).
3. Upgrade TUI rendering parity with rich formatted output.
